---

http:
  middlewares:
    authelia:
      forwardAuth:
        address: 'https://authelia.{{ env "SERVICES_DOMAIN" }}/api/authz/forward-auth'
        trustForwardHeader: true
        authResponseHeaders:
          - 'Remote-User'
          - 'Remote-Groups'
          - 'Remote-Email'
          - 'Remote-Name'

...

